{
  "name": "emily-tang-portfolio",
  "version": "1.0.0",
  "scripts": {
    "start": "serve",
    "build": "mkdir -p dist && cp index.html index.tsx dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
